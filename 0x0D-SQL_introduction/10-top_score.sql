-- Lists all records of the table 'second table` of the database 'hbtn_0c_0'
-- in the MySQL server.
-- Record displays both score and name
-- Result is ordered by score (top first)
SELECT score, name FROM `second_table`
ORDER BY score DESC;

